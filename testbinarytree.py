import unittest
import binarytreefile

class TestBTree(unittest.TestCase):

    def test_root(self):
        return 0
        #Create a node, test that the node now have a value


    def test_addNode(self):
        #Add a second node, test if it gets to the right side
        return 0
        
